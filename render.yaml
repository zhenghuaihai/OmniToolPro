services:
  - type: web
    name: omnitool-pro
    env: docker
    plan: free
    region: singapore
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
      - key: PORT
        value: 8000
